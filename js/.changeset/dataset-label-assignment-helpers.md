---
"@arizeai/phoenix-client": minor
---

Add typed dataset-label assignment helpers to the `datasets` subpath. Callers can list, idempotently add or remove, and fully replace the labels on a dataset selected by name or GlobalID. Empty replacement arrays clear all labels.
